---
# vars file for aspects_mysql_server
aspects_common_packages:
  pythonmysqldb:
    state: "latest"
  mysqlserver:
    # don't want to trigger a restart when just adding db's or users, so just present instead of latest.
    state: "present"